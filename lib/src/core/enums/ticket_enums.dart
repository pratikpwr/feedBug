enum TicketType {
  bug,
  improvement,
}

enum TicketPriority {
  highest,
  high,
  medium,
  low,
  lowest,
}

enum TicketStatus {
  created,
  willDo,
  notDoing,
  inProgress,
  nextTime,
  done,
}
